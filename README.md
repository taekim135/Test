# React + Vite

FULLSTACK Open Frontend Template/Examples


Vitest and the jsdom library = testing React components. Vitest is a Vite-native testing framework that provides a fast and efficient way to run tests & report results, while jsdom is a JavaScript implementation of the DOM and HTML standards, allowing tests to simulate a browser environment.

Vitest: Provides test structures like describe(), test(), it(), expect()